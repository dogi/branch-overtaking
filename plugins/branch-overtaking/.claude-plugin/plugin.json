{
  "name": "branch-overtaking",
  "description": "Adds an overtaking skill: resolve the repo and branch this session was opened with, find the oldest open PR on that branch, spawn a session bound to it, and subscribe to the PR so its CI failures and review comments reach the session — plus the stale same-named local lineage check before building on a branch.",
  "version": "1.1.0",
  "author": {
    "name": "dogi"
  }
}
